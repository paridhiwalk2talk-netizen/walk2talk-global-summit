{
  "name": "walk2talk-static-site",
  "private": true,
  "description": "Static HTML/CSS/JS site. No framework, no backend. Vite is used only as a local preview server — deployment needs no build step.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
